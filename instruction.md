I need a robust offline drift monitoring pipeline connecting our machine learning operations and real-time backend endpoints. The system relies on a unified process where offline scripts train a tabular classifier and generate baseline distribution datasets across features. This data must be serialized and stored natively to drive real-time API behavior.

Your solution will be assessed on its ability to parse streaming HTTP POST payloads containing input datasets natively in Django. The endpoints must validate inputs comprehensively, returning standard HTTP rejection codes if schemas or data formats are mismatched, and successfully compute predictive outcomes from the stored model. 

Additionally, the system must detect feature drift by securely evaluating incoming batch distributions against the initial training baselines, producing drift metrics payloads so downstream engineering teams can monitor model degradation. You must ensure the drift calculations are mathematically exact and numerically robust against anomalies like missing or empty bins.
