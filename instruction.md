Hey there, I need you to implement an offline drift monitoring pipeline for our machine learning project! 

The system should have two main pieces. First, I need a Python CLI located at `/app/environment/cli/train.py`. This script should train a tabular `RandomForestClassifier` from a provided CSV data path (using the last column as the target), log the model's accuracy and confusion matrix to Weights & Biases locally by setting the WandB mode to offline, and then save the serialized `joblib` model artifact to a specified model output path. While training, you also need to compute 10-bin equal-width histograms for each of the numerical features and save the bin edges and training set counts to a JSON output path.

Second, I need you to extend our API backend located in `/app/environment/django_project/api/views.py`. Set up a `/api/predict/` endpoint that receives a list of feature values, loads the local model, and returns the prediction integer. Then, add a `/api/drift/` endpoint that accepts a batch of feature arrays and computes the Population Stability Index (PSI) against the saved training histograms you produced during the CLI step. The drift endpoint should calculate and return the PSI for each feature by comparing the actual batch distributions against the expected training distributions (be sure to add a small epsilon like `1e-6` to avoid division by zero errors). If any feature's PSI exceeds 0.1, flag the entire batch as drifted.

Make sure you validate the inputs appropriately and gracefully return standard HTTP 400 Bad Request error dictionaries if schemas or data formats are mismatched!
